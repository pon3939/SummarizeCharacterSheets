{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "install libraries",
      "type": "shell",
      "command": "${workspaceFolder}/source/.venv/bin/pip install --upgrade -r ${workspaceFolder}/source/requirements_dev.txt -t ${workspaceFolder}/source/layers"
    }
  ]
}
