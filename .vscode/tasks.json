{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "install libraries",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/pip install --upgrade -r ${workspaceFolder}/requirements_dev.txt -t ${workspaceFolder}/layers"
    }
  ]
}
